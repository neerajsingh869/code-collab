# Get your keys at liveblocks.io -> your project -> API Keys.
# Only the secret key is needed: the browser never talks to Liveblocks with a
# key of its own, it calls /api/liveblocks-auth for a session instead.
LIVEBLOCKS_SECRET_KEY=sk_your_key_here
